// index.js (React Native)
import { AppRegistry } from 'react-native';
import App from './App';
import { name as appName } from './app.json'; // auto-created by React Native CLI or Expo

AppRegistry.registerComponent(appName, () => App);
